# Company filter (legacy SSR)

`company_filter.php` — HTML fragment + script tag for legacy PHP pages.

SPA uses `frontend/src/utils/company/sharedCompanyFilter.js` instead; no PHP `require` references this file.
